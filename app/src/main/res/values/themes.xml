<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidLab" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidLab" parent="Base.Theme.AndroidLab" />

    <!--로딩창-->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
<!--    <style name="Theme.AndroidLab.SplashScreen" parent="Theme.AndroidLab">-->
        <item name="windowSplashScreenBackground">@color/white</item> <!-- 배경색 -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/new_loading</item> <!-- 로딩 아이콘 -->
<!--        <item name="windowSplashScreenAnimationDuration">1000</item> &lt;!&ndash; 지속 시간(ms) &ndash;&gt;-->
        <item name="postSplashScreenTheme">@style/Theme.AndroidLab</item>
    </style>

</resources>